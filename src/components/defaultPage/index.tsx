import React from 'react';
import { PageContainer } from '@ant-design/pro-layout';

const DefaultPage: React.FC = (props: any) => {
  return <PageContainer>
    <div style={{ height: '100vh' }}>即将到来，敬请期待…</div>
  </PageContainer>
};

export default DefaultPage;
